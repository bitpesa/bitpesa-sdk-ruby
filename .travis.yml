language: ruby
cache: bundler
rvm:
  - 2.3
  - 2.4
  - 2.5
script:
  - bundle install --path vendor/bundle
  - bundle exec rspec
  - gem build bitpesa-sdk.gemspec
  - gem install ./bitpesa-sdk-0.3.0.gem
